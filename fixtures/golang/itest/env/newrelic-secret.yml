apiVersion: v1
kind: Secret
metadata:
  name: {{.Env "APP_NAME"}}-newrelic-secret
  labels:
    name: {{.Env "APP_NAME"}}-newrelic-secret
    app: {{.Env "APP_NAME"}}
type: Opaque
data:
  licensekey: {{`{{`}}.ExpandEnv "${APP_SECRETS_ROOT}/newrelic/licensekey" | .Cat | .ToBase64{{`}}`}}